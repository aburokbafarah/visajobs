import Parse from './config/parseConfig';
import Components from './components/Components';

// App is the root component, routing setup lives in Components
function App() {
  return <Components />;
}

export default App;